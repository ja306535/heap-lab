#include <stdio.h>//include the library that is needed to make the code run
#include <stdlib.h>//include the <stdlib.h> library, so we can use dynamic memory
//allocation!
//function headers for the two functions required for this program
int isHeap_recursive(int arr[], int i, int n);
int isHeap_iter(int arr[], int n);